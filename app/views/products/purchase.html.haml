.container
  %h1 Item Entry
  = simple_form_for @product, :html => {:class => 'form-horizontal' } do |f|

    = f.input :added_no_of_items

    .form-actions
      = f.button :submit, 'Submit', :class=> "btn btn-primary"
